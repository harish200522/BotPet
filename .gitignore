node_modules/

# Rust build output — several GB, entirely regenerable
target/
**/target/

# scratch crate used to check a dependency in isolation; has its own .git
cargotest/

# `tauri icon` also emits mobile icon sets; this is a desktop-only build
badkat/src-tauri/icons/android/
badkat/src-tauri/icons/ios/

# screenshots and logs from the test harnesses
.shot-*.png
.shot-*.json
*.log
.badkat*.log
.morphcat*.log

.DS_Store
Thumbs.db

# secrets — never commit this, whatever else changes here
CREDENTIALS.md
